[oceandb]

module = ${DB_MODULE}
db.hostname = ${DB_HOSTNAME}
db.port = ${DB_PORT}

############################################# ELASTIC #############################################
db.index = ${DB_INDEX}

[resources]
; These constitute part of the aquarius url which is used in setting the `api_url` in the `OceanContractsWrapper`
aquarius.url = ${AQUARIUS_BIND_URL}
allowFreeAssetsOnly=${ALLOW_FREE_ASSETS_ONLY}
